package controller;

/**
 * Resultados de operaciones de los controladores.
 */
public enum ControllerResult {
    SUCCESS,
    INVALID_INPUT,
    INVALID_MOVE,
    TURN_SKIPPED,
    PASS_NOT_ALLOWED,
    GAME_OVER,
    NO_ACTIVE_GAME,
    LOAD_NOT_FOUND,
    NOT_FOUND
}
